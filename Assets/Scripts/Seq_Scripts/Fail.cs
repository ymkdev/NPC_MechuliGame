using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fail : MonoBehaviour
{
    public static bool failGoal;

    // Start is called before the first frame update
    void Start()
    {
        failGoal = false;
    }

    void Update()
    {
    }
}
