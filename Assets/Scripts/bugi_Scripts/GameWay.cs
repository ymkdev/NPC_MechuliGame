using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameWay : MonoBehaviour
{
    public static bool tf;

    // Start is called before the first frame update
    void Start()
    {
        tf = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
